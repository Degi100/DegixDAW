# Daily Snapshot Edge Function - Environment Variables
#
# These must be set in Supabase Dashboard → Edge Functions → Settings
# NOT in .env.local (that's for the frontend)

# Supabase connection
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Cron authentication (optional but HIGHLY recommended)
# Generate a random secret: openssl rand -base64 32
CRON_SECRET=your-random-secret-here
